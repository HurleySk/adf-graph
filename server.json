{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.HurleySk/adf-graph",
  "description": "MCP server that builds a queryable dependency graph from ADF pipeline artifacts",
  "repository": {
    "url": "https://github.com/HurleySk/adf-graph",
    "source": "github"
  },
  "version": "0.5.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "adf-graph",
      "version": "0.5.0",
      "transport": {
        "type": "stdio"
      },
      "tools": [
        { "name": "graph_stats", "description": "Node/edge counts by type, build time, staleness flag" },
        { "name": "graph_find_consumers", "description": "All pipeline activities that read/write/call a given artifact" },
        { "name": "graph_describe_pipeline", "description": "Pipeline summary, activity list, or full detail with column mappings" },
        { "name": "graph_impact_analysis", "description": "All nodes affected upstream/downstream if an artifact changes" },
        { "name": "graph_data_lineage", "description": "Data flow paths for a Dataverse entity or staging table with optional column filter" },
        { "name": "graph_find_paths", "description": "All dependency paths between two named nodes" },
        { "name": "graph_search_queries", "description": "Search across activity SQL, FetchXML, stored procedure names/parameters, and ExecutePipeline parameter values" },
        { "name": "graph_diff_pipeline", "description": "Compare a pipeline's structure across two environments" },
        { "name": "graph_list_environments", "description": "List all configured environments with paths, stats, and staleness" },
        { "name": "graph_add_overlay", "description": "Add an overlay path to an environment for merged graph views" },
        { "name": "graph_remove_overlay", "description": "Remove a runtime overlay from an environment" },
        { "name": "graph_list_overlays", "description": "List all overlays for an environment" },
        { "name": "graph_add_environment", "description": "Register a new ephemeral environment pointing to an ADF root" },
        { "name": "graph_remove_environment", "description": "Remove a runtime environment" }
      ],
      "environmentVariables": [
        {
          "name": "ADF_ROOT",
          "description": "Path to directory containing ADF artifacts (pipeline/, dataset/, etc.). Used for single-environment setup.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "ADF_CONFIG",
          "description": "Path to adf-graph.json config file for multi-environment setup. Takes priority over ADF_ROOT.",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
